#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Good)
 */

int main(void)
{
	char f, l;

	f = 'a';
	l = 'A';
	while
		(f <= 'z') {
			putchar(f);
			f++;
		}
	while
		(l <= 'Z') {
			putchar(l);
			l++;
		}
	putchar('\n');
	return (0);
}
