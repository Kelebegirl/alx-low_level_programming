#include "main.h"

/**
 * _abs - function that return thee absolute value of given integers
 * @d: declaration of input function
 * Return: 0 (Good)
 */

int _abs(int d)
{
	if (d < 0)
		d = (-1) * d;
	return (d);
}
